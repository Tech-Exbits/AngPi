import { PcDirective } from './pc.directive';

describe('PcDirective', () => {
  it('should create an instance', () => {
    const directive = new PcDirective();
    expect(directive).toBeTruthy();
  });
});
