import { BetaAccessDirective } from './beta-access.directive';

describe('BetaAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new BetaAccessDirective();
    expect(directive).toBeTruthy();
  });
});
