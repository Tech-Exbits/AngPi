import { IosDirective } from './ios.directive';

describe('IosDirective', () => {
  it('should create an instance', () => {
    const directive = new IosDirective();
    expect(directive).toBeTruthy();
  });
});
