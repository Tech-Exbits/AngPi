import { AndriodDirective } from './andriod.directive';

describe('AndriodDirective', () => {
  it('should create an instance', () => {
    const directive = new AndriodDirective();
    expect(directive).toBeTruthy();
  });
});
