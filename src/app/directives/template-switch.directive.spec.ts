import { TemplateSwitchDirective } from './template-switch.directive';

describe('TemplateSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
