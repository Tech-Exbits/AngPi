import { AccessGrantedDirective } from './access-granted.directive';

describe('AccessGrantedDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessGrantedDirective();
    expect(directive).toBeTruthy();
  });
});
