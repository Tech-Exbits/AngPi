import { AccessDeniedDirective } from './access-denied.directive';

describe('AccessDeniedDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessDeniedDirective();
    expect(directive).toBeTruthy();
  });
});
